import 'package:flutter/material.dart';
import 'package:jet_set_go/travel_tips.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: TravelTipsApp(), // Directly launch the Document Upload Page
  ));
}