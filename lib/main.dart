import 'package:flutter/material.dart';
import 'package:test_flutter/document_selection_page.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: DocumentSelectionPage(), // Directly launch the Document Upload Page
  ));
}